"""실시간 스트리밍 시뮬레이터 + 슬라이딩 윈도우 버퍼.  ⚠️ 미구현(빈 골격)

[현재 상태]
  - 아직 알맹이 없음. docstring(설명)만 있는 placeholder(자리 표시) 파일이다.
  - 어디서도 import 하지 않으므로 있어도/없어도 동작에 영향 없다.
  - TASKS.md 의 T5.1(스트리밍 시뮬레이터)에 해당하는 '향후 과제' 자리.

[왜 남겨뒀나]
  현재 파이프라인은 '파일 한 개를 통째로' 분석한다(배치 방식).
  실제 현장은 센서에서 신호가 '실시간으로 조금씩' 들어온다(스트리밍 방식).
  이 파일은 그 실시간 상황을 흉내 내는 자리이며, 발표에선 '향후 확장'으로 설명한다.

[채운다면 — 무엇을 만들면 되나]
  StreamSimulator(signal, window_length, overlap):
    저장된 신호를 한 점씩(또는 묶음씩) 흘려보내며,
    링버퍼(ring buffer = 고정 크기 큐)가 window_length 만큼 차면
    한 윈도우를 잘라 내보내는 '제너레이터(generator)'.
    → 이 윈도우를 inference 파이프라인(run_recording 등)에 넘기면
       파일 분석이 아니라 '실시간 스트림 분석'이 된다.

  핵심 개념:
    - 링버퍼 : 새 값이 들어오면 가장 오래된 값을 밀어내는 고정 길이 버퍼.
    - overlap: 윈도우를 절반씩 겹쳐 내보내 경계에 걸친 결함을 놓치지 않게 함.
    - 제너레이터: 한 번에 다 만들지 않고 필요할 때마다 윈도우를 하나씩 yield.

  주의:
    - 윈도우 길이·overlap 은 직접 정하지 말고 cfg["window"] 값을 그대로 써서
      배치 분석과 '완전히 동일한 전처리'를 유지할 것(결과 일관성).

TODO: StreamSimulator(signal, window_length, overlap) -> 윈도우 제너레이터.
"""